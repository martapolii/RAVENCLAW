// handle errors

function handleError(req, res) {
  // code to handle the error - implement if needed
}
function getErrorMessage(errMsg) {
  console.log(errMsg);
  
} // Export the controller function
export default {
  handleError: handleError,
  getErrorMessage: getErrorMessage,
};
